server:
  port: 9004
spring:
  application:
    name: spring-cloud-config
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/LuckLarry/spring-config-server-training
          search-paths: config-repo
#          username:
#          password: 如果是公有库就不需要账号和密码
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/
      #注册中心